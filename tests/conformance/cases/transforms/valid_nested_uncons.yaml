spec_version: "0.0"
types:
  Cup:
    domain: object
processes:
  uncons2:
    kind: atomic
    inputs:
      xs:
        type: Array<Array<Cup>>
        phase: data
    outputs:
      head:
        type: Array<Cup>
        phase: data
      tail:
        type: Array<Array<Cup>>
        phase: data
    objects:
      transform:
        - kind: array_uncons
          inputs:
            xs: inputs.xs
          outputs:
            head: outputs.head
            tail: outputs.tail
  main:
    kind: atomic
    inputs: {}
    outputs: {}
entry: main
