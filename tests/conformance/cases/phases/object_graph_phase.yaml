spec_version: "0.0"
types:
  Cup:
    domain: object
processes:
  main:
    kind: atomic
    inputs:
      cup:
        type: Cup
        phase: graph
    outputs:
      cup:
        type: Cup
        phase: graph
    objects:
      map:
        outputs.cup: inputs.cup
entry: main
