spec_version: "0.0"
processes:
  main:
    kind: atomic
    inputs:
      x:
        type: Int
        phase: data
    outputs: {}
    contracts:
      requires:
        - expr: "inputs.x.view and inputs.x.view"
entry: main
