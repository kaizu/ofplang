spec_version: "0.0"
processes:
  main:
    kind: atomic
    inputs:
      x:
        type: Int
        phase: data
    outputs:
      y:
        type: Int
        phase: data
    contracts:
      ensures:
        - expr: "outputs.y.view >= inputs.x.view"
entry: main
