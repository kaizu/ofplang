spec_version: "0.0"
processes:
  main:
    kind: atomic
    inputs:
      x:
        type: Int
        phase: data
    outputs: {}
    contracts:
      requires:
        - expr: "1 < inputs.x.view < 10"
entry: main
